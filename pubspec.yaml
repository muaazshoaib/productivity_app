name: productivity_app

description: "Productivity App - Final Year Project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  firebase_auth:
  firebase_core:
  cloud_firestore:
  flutter_dnd:
  flutter_switch:
  flutter_inset_box_shadow:
  hive_flutter:
  flutter_slidable:
  flutter_heatmap_calendar:
  intl:
  shared_preferences:
  animated_snack_bar:
  google_generative_ai:
  table_calendar:
  flutter_staggered_grid_view:
  sqflite:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true

  assets:
    - assets/images/
